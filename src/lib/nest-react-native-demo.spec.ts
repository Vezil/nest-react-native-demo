import { nestReactNativeDemo } from './nest-react-native-demo';

describe('nestReactNativeDemo', () => {
  it('should work', () => {
    expect(nestReactNativeDemo()).toEqual('nest-react-native-demo');
  });
});
